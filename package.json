{
  "name": "hero-searcher-monorepo",
  "version": "1.0.0",
  "description": "HeroSearcher - Professional Portfolio Project with SQLite, React, and Express",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd api && npm install --legacy-peer-deps && cd ../client && npm install --legacy-peer-deps",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:client\"",
    "dev:api": "cd api && npm run dev",
    "dev:client": "cd client && npm start",
    "build": "cd client && npm run build",
    "test": "cd client && npm test"
  },
  "keywords": [
    "hero",
    "searcher",
    "react",
    "express",
    "sqlite",
    "monorepo"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
